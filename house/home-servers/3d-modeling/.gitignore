# build123d / Python середовище — не комітимо
.venv/
__pycache__/
*.pyc

# out/ — згенеровані артефакти (STEP/STL/PNG): відтворюються скриптами
# cad/*.py, а важать десятки МБ (tray.step 74М!) — у git не тримаємо.
# ВИНЯТОК: print_all.3mf — готовий файл для принтера (2МБ, 3 пластини,
# всі деталі) — тримаємо, щоб забирати на MacBook без ребілду.
out/*
!out/print_all.3mf
.DS_Store
