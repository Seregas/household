# build123d / Python середовище — не комітимо
.venv/
__pycache__/
*.pyc

# out/ — повністю згенеровані артефакти (STEP/STL/PNG): відтворюються
# скриптами cad/*.py за секунди, а важать десятки МБ — у git не тримаємо.
out/
.DS_Store
